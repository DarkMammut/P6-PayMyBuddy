services:
  mysql:
    image: 'mysql:latest'
    environment:
      - 'MYSQL_DATABASE=paymybuddy'
      - 'MYSQL_PASSWORD=admin'
      - 'MYSQL_ROOT_PASSWORD=root'
      - 'MYSQL_USER=admin'
    ports:
      - '3306'
